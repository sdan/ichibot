import { Config } from ".";

const config: Config = {
  apiKey: '',
  apiSecret: '',
  // subAccount: '', // On FTX the name of the subaccount (url-encoded if relevant). Omit if not using a sub account.
}

export default config;
